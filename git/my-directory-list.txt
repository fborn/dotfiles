The file bash_aliases contains:
#!/usr/bin/env bash

alias gall='git add --all'
alias gc='git commit'
alias gca='git commit -a'
alias gcpa='git commit -a "$@" && git push origin master'
alias gp='git push origin'
alias gf='git fetch origin'
alias gpl='git pull origin'
alias gch='git checkout'
alias gb='git branch'
alias gs='git status -sb' # upgrade your git if -sb breaks for you. it's fun.

The file bash_completion contains:
#!/usr/bin/env bash

if [ -f `brew --prefix`/etc/bash_completion ]; then
  . `brew --prefix`/etc/bash_completion
fi

The file brew_packages contains:
#!/usr/bin/env bash

# brew installer helper, normally run this from 'dot'
TMPPATH=$PATH
PATH=/usr/local/bin:$PATH

brew install git || brew upgrade git

export PATH=$TMPPATH

The file gitconfig.symlink contains:
[user]
    name = Ferenc Born
    email = fborn@schubergphilis.com
[color]
    diff = auto
    status = auto
    branch = auto
    ui = true
[core]
    excludesfile = ~/.gitignore
    editor = $EDITOR
[push]
    default = simple

The file gitconfig.symlink.example contains:
[user]
    name = AUTHORNAME
    email = AUTHOREMAIL
[color]
    diff = auto
    status = auto
    branch = auto
    ui = true
[core]
    excludesfile = ~/.gitignore
    editor = $EDITOR
[push]
    default = simple

The file gitignore.symlink contains:
.DS_Store
*~
*.swp

